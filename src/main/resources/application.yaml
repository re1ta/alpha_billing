spring:
  application:
    name: billing
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://localhost:5432/postgres
    password: postgres
  jpa:
    database: postgres
    hibernate:
      ddl-auto: update
    show-sql: 'true'